# Generated by [Kustohelmize](https://github.com/yeahdongcn/kustohelmize)
apiVersion: apps/v1
kind: Deployment
metadata: 
  name: {{ include "mychart.fullname" . }}
  namespace: default
spec: 
  replicas: 2
  selector: 
    matchLabels: 
      app: nginx
  template: 
    metadata: 
      labels: 
        app: nginx
    spec: 
      containers:         
        - name: nginx
          image: nginx:latest
          ports:             
            {{- if not .Values.http.enabled }}
            - containerPort: 80
              name: http
              protocol: TCP
            {{- end }}
            {{- if not .Values.https.enabled }}
            - containerPort: 443
              name: https
              protocol: TCP
            {{- end }}
