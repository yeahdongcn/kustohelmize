# Generated by [Kustohelmize](https://github.com/yeahdongcn/kustohelmize)
apiVersion: apps/v1
kind: Deployment
metadata: 
  name: {{ include "mychart.fullname" . }}
  namespace: default
spec: 
  replicas: 2
  selector: 
    matchLabels: 
      app: nginx
  template: 
    metadata: 
      labels: 
        app: nginx
    spec: 
      containers:         
        - name: nginx
          image: nginx:latest
          args: 
            {{- if .Values.arg0.enabled }}
            - arg0
            {{- end }}
            {{- if not .Values.arg0.enabled }}
            - arg1
            {{- end }}
          ports:             
            {{- if .Values.http.enabled }}
            - containerPort: 80
              name: http
              protocol: TCP
            {{- end }}
            {{- if .Values.https.enabled }}
            - containerPort: 443
              name: https
              protocol: TCP
            {{- end }}
            - containerPort: 9443
              name: webhook-server
              protocol: TCP
