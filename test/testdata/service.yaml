apiVersion: v1
kind: Service
metadata:
  name: mychart-service
  labels:
    app: mychart
spec:
  type: ClusterIP
  ports:
    - port: 9001
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: mychart
